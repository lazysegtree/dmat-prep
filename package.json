{
  "name": "dmat-prep-ui-tests-poc",
  "private": true,
  "type": "module",
  "scripts": {
    "test:unit": "node --test tests/unit/*.test.js",
    "test:ui": "playwright test",
    "test": "npm run test:unit && npm run test:ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
